1. We are making an article front-end consisting of title , description , image , content. 
2. Make sure you give a title to webpage as Article (not article title) in the <title> tag which is in <head>
3. Do not make in any changes in already proving classes and tags in scaffold file .
4. make a divs with classes named  as article-title (name is as Airport it mendatory), article-description(write anything), article-img-container,article-content.
5. all these divs should be inside the container-article which has been already given . 
6. Add <h1> inside article-title while naming it , add <h3> inside article-description , add <img> inside article-img-container>, add <p> inbside article-content  